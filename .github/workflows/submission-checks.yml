name: Preserve submitted release
on: [push, pull_request]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm run check:submission
      - run: npm run audit:hosted-final
      - run: npm run audit:b-fault-final
      - run: npm test
      - run: npm run test:types
      - run: npm run build
